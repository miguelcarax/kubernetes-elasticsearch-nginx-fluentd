# kubernetes-elasticsearch-nginx-fluentd
Example of an application to deploy an nginx server with an fluentd sidecar container who sends log entries to an elasticsearch 

---

### How to deploy

```
$ kubectl apply -f .
```
